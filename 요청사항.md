<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NamDev | Innovative IT Solutions</title>
    <style>
        /* 기본 스타일 및 다크 모드 테마 */
        :root {
            --bg-color: #0f172a;
            --text-color: #f8fafc;
            --accent-color: #38bdf8;
            --accent-hover: #0ea5e9;
            --card-bg: #1e293b;
            --header-bg: rgba(15, 23, 42, 0.9);
        }
        
        * {
            box-sizing: border-box;
        }

        body, html {
            margin: 0;
            padding: 0;
            font-family: 'Pretendard', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            background-color: var(--bg-color);
            color: var(--text-color);
            scroll-behavior: smooth;
            line-height: 1.6;
        }

        /* 네비게이션 바 */
        header {
            position: fixed;
            top: 0;
            width: 100%;
            background: var(--header-bg);
            backdrop-filter: blur(12px);
            padding: 20px 0;
            z-index: 1000;
            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
        }

        .nav-container {
            max-width: 1200px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 20px;
        }

        .logo {
            font-size: 1.5rem;
            font-weight: 800;
            color: var(--text-color);
            text-decoration: none;
            letter-spacing: -0.5px;
        }

        .logo span {
            color: var(--accent-color);
        }
        
        .nav-links a {
            color: #cbd5e1;
            text-decoration: none;
            margin-left: 30px;
            font-weight: 500;
            font-size: 0.95rem;
            transition: color 0.3s ease;
        }
        
        .nav-links a:hover {
            color: var(--accent-color);
        }

        /* 공통 섹션 스타일 */
        section {
            padding: 120px 20px 80px;
            max-width: 1200px;
            margin: 0 auto;
        }

        .section-header {
            text-align: center;
            margin-bottom: 50px;
        }

        .section-header h2 {
            color: var(--text-color);
            font-size: 2.2rem;
            margin-bottom: 15px;
            position: relative;
            display: inline-block;
        }

        .section-header h2::after {
            content: '';
            position: absolute;
            width: 40px;
            height: 4px;
            background-color: var(--accent-color);
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%);
            border-radius: 2px;
        }

        .section-header p {
            color: #94a3b8;
            font-size: 1.1rem;
            max-width: 600px;
            margin: 20px auto 0;
        }

        /* 히어로(메인) 섹션 */
        #home {
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding-top: 60px;
            background: radial-gradient(circle at 50% 50%, rgba(56, 189, 248, 0.1) 0%, transparent 50%);
        }
        
        #home h1 {
            font-size: 4.5rem;
            margin: 0;
            line-height: 1.2;
            letter-spacing: -1.5px;
            font-weight: 800;
        }

        #home h1 span {
            background: linear-gradient(135deg, #38bdf8, #818cf8);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        
        #home > p {
            font-size: 1.25rem;
            color: #94a3b8;
            margin-top: 25px;
            max-width: 700px;
            line-height: 1.8;
        }

        .btn-primary {
            display: inline-block;
            background-color: var(--accent-color);
            color: #0f172a;
            padding: 18px 40px;
            text-decoration: none;
            font-weight: 700;
            border-radius: 8px;
            margin-top: 40px;
            transition: all 0.3s ease;
            font-size: 1.1rem;
            border: none;
            cursor: pointer;
        }
        
        .btn-primary:hover {
            background-color: var(--accent-hover);
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(56, 189, 248, 0.3);
        }

        .btn-outline {
            background-color: transparent;
            color: var(--accent-color);
            border: 2px solid var(--accent-color);
        }

        .btn-outline:hover {
            background-color: rgba(56, 189, 248, 0.1);
            color: var(--accent-color);
            box-shadow: none;
        }

        /* 그리드 컨테이너 */
        .grid-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 30px;
        }
        
        .card {
            background-color: var(--card-bg);
            padding: 40px 30px;
            border-radius: 16px;
            border: 1px solid rgba(255, 255, 255, 0.03);
            transition: transform 0.3s ease, box-shadow 0.3s ease, opacity 0.4s ease;
            position: relative;
            overflow: hidden;
        }

        .card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: linear-gradient(90deg, var(--accent-color), transparent);
            opacity: 0;
            transition: opacity 0.3s ease;
        }
        
        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 40px -10px rgba(0, 0, 0, 0.4);
            border-color: rgba(56, 189, 248, 0.2);
        }

        .card:hover::before {
            opacity: 1;
        }

        .card h3 {
            margin-top: 0;
            font-size: 1.4rem;
            color: var(--text-color);
            margin-bottom: 15px;
        }

        .card p {
            color: #94a3b8;
            margin-bottom: 0;
            font-size: 1rem;
            line-height: 1.7;
        }

        /* 포트폴리오 태그 필터 */
        .filter-container {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 15px;
            margin-bottom: 40px;
        }

        .filter-btn {
            background-color: var(--card-bg);
            color: #cbd5e1;
            border: 1px solid rgba(255, 255, 255, 0.1);
            padding: 10px 25px;
            border-radius: 30px;
            font-size: 0.95rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .filter-btn:hover {
            border-color: var(--accent-color);
            color: var(--accent-color);
        }

        .filter-btn.active {
            background-color: var(--accent-color);
            color: #0f172a;
            border-color: var(--accent-color);
        }

        .work-card {
            /* JS 애니메이션을 위해 추가 */
            display: block;
        }

        .work-tag {
            display: inline-block;
            font-size: 0.8rem;
            color: var(--accent-color);
            background: rgba(56, 189, 248, 0.1);
            padding: 4px 10px;
            border-radius: 4px;
            margin-bottom: 15px;
            font-weight: bold;
        }

        /* CTA / Contact 섹션 (폼 추가) */
        .contact-wrapper {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 50px;
            background-color: var(--card-bg);
            border-radius: 24px;
            padding: 60px;
            border: 1px solid rgba(255,255,255,0.05);
        }

        .contact-info h2 {
            font-size: 2.2rem;
            margin-bottom: 20px;
            margin-top: 0;
        }

        .contact-info p {
            color: #94a3b8;
            font-size: 1.1rem;
            margin-bottom: 30px;
        }

        .email-display {
            font-size: 1.4rem;
            font-weight: bold;
            color: var(--accent-color);
            display: inline-block;
            margin-bottom: 20px;
            padding: 15px 25px;
            background: rgba(15, 23, 42, 0.5);
            border-radius: 8px;
            border: 1px dashed rgba(56, 189, 248, 0.3);
        }

        /* 폼 스타일 */
        .contact-form {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .form-group {
            display: flex;
            flex-direction: column;
            text-align: left;
        }

        .form-group label {
            font-size: 0.9rem;
            color: #cbd5e1;
            margin-bottom: 8px;
            font-weight: 600;
        }

        .contact-form input, 
        .contact-form textarea {
            width: 100%;
            padding: 16px;
            background-color: rgba(15, 23, 42, 0.6);
            border: 1px solid rgba(255,255,255,0.1);
            border-radius: 8px;
            color: var(--text-color);
            font-family: inherit;
            font-size: 1rem;
            transition: border-color 0.3s ease;
        }

        .contact-form input:focus, 
        .contact-form textarea:focus {
            outline: none;
            border-color: var(--accent-color);
        }

        .contact-form button {
            margin-top: 10px;
            width: 100%;
        }

        /* 모바일 반응형 처리 */
        @media (max-width: 768px) {
            #home h1 { font-size: 2.8rem; }
            .nav-links { display: none; }
            .contact-wrapper { grid-template-columns: 1fr; padding: 30px; }
        }
    </style>
</head>
<body>

    <header>
        <div class="nav-container">
            <a href="#home" class="logo">Nam<span>Dev</span></a>
            <div class="nav-links">
                <a href="#about">About</a>
                <a href="#works">Works</a>
                <a href="#services">Services</a>
                <a href="#contact">Contact</a>
            </div>
        </div>
    </header>

    <section id="home">
        <h1>Building Innovative<br><span>Digital Experiences</span></h1>
        <p>NamDev는 웹, 모바일 앱, 그리고 인터랙티브 콘텐츠의 경계를 허무는 통합 IT 파트너입니다.<br>당신의 아이디어를 가장 완벽한 코드로 구현합니다.</p>
        
        <div style="display: flex; gap: 15px; justify-content: center; flex-wrap: wrap;">
            <a href="#about" class="btn-primary">NamDev 소개 보기</a>
            <a href="#works" class="btn-primary btn-outline">포트폴리오 보기</a>
        </div>
    </section>

    <section id="about">
        <div class="section-header">
            <h2>Who is NamDev?</h2>
            <p>단순한 코딩을 넘어, 클라이언트의 비즈니스 가치를 극대화하는 솔루션을 설계합니다.</p>
        </div>
        
        <div class="grid-container">
            <div class="card">
                <span style="font-size: 2.5rem; display: block; margin-bottom: 20px;">🎯</span>
                <h3>비즈니스 중심 아키텍처</h3>
                <p>기획 단계부터 비즈니스 모델을 분석하여, 확장성과 유지보수성이 뛰어난 최적의 시스템 아키텍처를 설계합니다.</p>
            </div>
            <div class="card">
                <span style="font-size: 2.5rem; display: block; margin-bottom: 20px;">⚡</span>
                <h3>크로스 플랫폼 최적화</h3>
                <p>웹과 모바일의 경계를 허무는 하이브리드 기술을 통해 개발 비용을 절감하고 빠른 시장 진입(Time-to-Market)을 돕습니다.</p>
            </div>
            <div class="card">
                <span style="font-size: 2.5rem; display: block; margin-bottom: 20px;">🤝</span>
                <h3>투명한 커뮤니케이션</h3>
                <p>프로젝트 착수부터 완료까지 체계적인 마일스톤 관리와 투명한 소통으로 리스크를 최소화합니다.</p>
            </div>
        </div>
    </section>

    <!-- 포트폴리오 (태그 필터링 적용) -->
    <section id="works">
        <div class="section-header">
            <h2>Featured Works</h2>
            <p>분야별 태그를 클릭하여 NamDev의 프로젝트를 확인해 보세요.</p>
        </div>

        <!-- 태그 필터 버튼 -->
        <div class="filter-container">
            <button class="filter-btn active" onclick="filterProjects('all', this)">All</button>
            <button class="filter-btn" onclick="filterProjects('web', this)">Web (웹/SaaS)</button>
            <button class="filter-btn" onclick="filterProjects('app', this)">Mobile App</button>
            <button class="filter-btn" onclick="filterProjects('game', this)">3D & Game</button>
        </div>

        <div class="grid-container" id="portfolio-grid">
            <!-- 웹 프로젝트 -->
            <div class="card work-card" data-category="web">
                <span class="work-tag">Web / SaaS</span>
                <h3>글로벌 물류 트래킹 시스템</h3>
                <p>전 세계 물류 데이터를 실시간으로 수집하고 모니터링할 수 있는 반응형 웹 대시보드 및 백엔드 API 서버를 구축했습니다.</p>
            </div>
            <div class="card work-card" data-category="web">
                <span class="work-tag">Web</span>
                <h3>B2B 사내 그룹웨어</h3>
                <p>복잡한 인사 데이터를 시각화하고, 효율적인 업무 프로세스를 돕는 맞춤형 사내 인트라넷을 개발했습니다.</p>
            </div>
            
            <!-- 앱 프로젝트 -->
            <div class="card work-card" data-category="app">
                <span class="work-tag">Mobile App</span>
                <h3>O2O 매장 예약 하이브리드 앱</h3>
                <p>웹 기술 기반(Capacitor)으로 개발되어 초기 개발 비용을 40% 절감하면서도 네이티브 앱 수준의 퍼포먼스를 달성했습니다.</p>
            </div>
            <div class="card work-card" data-category="app">
                <span class="work-tag">Mobile App</span>
                <h3>피트니스 트래킹 앱</h3>
                <p>React Native를 활용하여 사용자의 운동 기록을 저장하고 분석해 주는 iOS/Android 크로스 플랫폼 앱을 구현했습니다.</p>
            </div>

            <!-- 게임/3D 프로젝트 -->
            <div class="card work-card" data-category="game">
                <span class="work-tag">3D & Game</span>
                <h3>신제품 런칭 3D 가상 쇼룸</h3>
                <p>Three.js를 활용하여 별도의 앱 설치 없이 브라우저에서 바로 체험 가능한 3D 가상 쇼룸을 제작하여 마케팅 전환율을 높였습니다.</p>
            </div>
            <div class="card work-card" data-category="game">
                <span class="work-tag">3D & Game</span>
                <h3>크로스 플랫폼 모바일 RPG</h3>
                <p>Unity와 WebGL을 활용해 개발된 게임으로, 복잡한 스테이지 기믹과 독창적인 턴제 AI 전투 시스템을 구현했습니다.</p>
            </div>
        </div>
    </section>

    <section id="services">
        <div class="section-header">
            <h2>Our Services</h2>
            <p>다양한 산업군에서 검증된 NamDev의 핵심 비즈니스 솔루션 영역입니다.</p>
        </div>
        <div class="grid-container">
            <div class="card">
                <h3>Enterprise Web Solutions</h3>
                <p>CRM, ERP 연동 등 복잡한 데이터를 다루는 고성능 웹 서비스 구축</p>
            </div>
            <div class="card">
                <h3>Cross-Platform Mobile Apps</h3>
                <p>iOS와 Android를 동시에 지원하는 효율적인 하이브리드 앱 개발</p>
            </div>
            <div class="card">
                <h3>Interactive & Game Contents</h3>
                <p>브랜드 경험을 강화하는 3D 웹 인터랙션 및 프로토타입 게임 제작</p>
            </div>
        </div>
    </section>

    <!-- 문의하기 폼 추가 -->
    <section id="contact">
        <div class="contact-wrapper">
            <div class="contact-info">
                <h2>Let's Talk!</h2>
                <p>프로젝트 기획 단계이신가요, 아니면 바로 개발 파트너가 필요하신가요? 아이디어를 남겨주시면 24시간 이내에 회신해 드립니다.</p>
                
                <div style="margin-top: 30px;">
                    <p style="margin-bottom: 5px; color: #cbd5e1; font-weight: bold;">다이렉트 이메일</p>
                    <div class="email-display">hello@namdev.company</div>
                </div>
            </div>

            <!-- 이메일 입력 폼 -->
            <form class="contact-form" onsubmit="event.preventDefault(); alert('문의가 성공적으로 전송되었습니다! (데모버전)');">
                <div class="form-group">
                    <label for="name">이름 / 회사명</label>
                    <input type="text" id="name" placeholder="홍길동 / (주)OO컴퍼니" required>
                </div>
                <div class="form-group">
                    <label for="email">답변 받으실 이메일 주소</label>
                    <input type="email" id="email" placeholder="example@email.com" required>
                </div>
                <div class="form-group">
                    <label for="message">프로젝트 내용 및 문의사항</label>
                    <textarea id="message" rows="5" placeholder="현재 구상 중인 프로젝트나 필요하신 서비스에 대해 자유롭게 적어주세요." required></textarea>
                </div>
                <button type="submit" class="btn-primary">문의 메시지 보내기</button>
            </form>
        </div>
    </section>

    <footer style="background-color: #0b1120; padding: 40px 20px; border-top: 1px solid rgba(255, 255, 255, 0.05); text-align: center; color: #64748b; font-size: 0.9rem;">
        &copy; 2026 NamDev Partners. All rights reserved.
    </footer>

    <!-- 태그 필터링 JavaScript -->
    <script>
        function filterProjects(category, btnElement) {
            // 1. 모든 버튼에서 active 클래스 제거하고 클릭된 버튼에만 추가
            const btns = document.querySelectorAll('.filter-btn');
            btns.forEach(btn => btn.classList.remove('active'));
            btnElement.classList.add('active');

            // 2. 카드 필터링 처리
            const cards = document.querySelectorAll('.work-card');
            cards.forEach(card => {
                // 부드러운 전환을 위해 일단 투명하게 만듦
                card.style.opacity = '0';
                
                setTimeout(() => {
                    if (category === 'all' || card.getAttribute('data-category') === category) {
                        card.style.display = 'block';
                        // display 변경 후 약간의 딜레이를 주어 opacity 애니메이션 적용
                        setTimeout(() => card.style.opacity = '1', 50);
                    } else {
                        card.style.display = 'none';
                    }
                }, 300); // 0.3초(CSS 트랜지션 시간) 후 display 속성 변경
            });
        }
    </script>
</body>
</html>